package org.as3.serializer.helpers
{
	import mx.utils.StringUtil;

	public class RefactoryClassDeclarations
	{
		/*public static function refactoryClassDeclaration(serializer:XML, originalPackage:String, newPackage:String):XML
		{
			retXML( serializer.toString().replace(originalPackage, newPackage) );
		}*/
	}
}  